# shimpz-browser

A real, undetectable Chrome driven over CDP inside an isolated container, exposed to the rest of the stack only through browser-agent's narrow, audited HTTP API.

---
Part of the **[Shimpz](https://github.com/roxygens/shimpz)** stack — a self-hosted, voice-driven autonomous agent.
